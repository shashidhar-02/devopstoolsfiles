#secret yaml
apiVersion: v1
kind: Secret
metadata:
  name: my-secret
type: Opaque
data:
  username: bXktdXNlcm5hbWU=  # base64 encoded 'my-username'
  password: bXktcGFzc3dvcmQ=  # base64 encoded 'my-password'
# Notes:
# Secret.yaml
# Types of Secrets (Opaque, TLS, Docker-registry)
# Base64 encoding and decoding of data
# Using Secrets in Pods (environment variables, volume mounts)
# Advanced Topics
# Managing Secrets with Kustomize
